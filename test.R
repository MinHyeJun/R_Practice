a <-1 # a에1 할당(변수에값을배정) =, <-, <<-
a # a 출력
b <-2
print(b)

a+b#연산가능
4/b
5*b

rm(b) # rm() 불필요한객체를메모리에서제거

var1 <-c(1, 2, 3, 4, 5) #숫자5개로구성된var1 생성
var1

1:5 # 1~5까지연속된일련번호를생성
var2 <-c(1:5) #1-5까지연속된값으로var2 생성
var2

var3 <-seq(1, 5) #1-5까지연속된값으로var2 생성
var3

var4 <-seq(1, 10, by = 2) #1~10까지2간격으로연속값으로var4 생성
var4